{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "code.fix-ci output",
  "description": "Reported via submit_step_progress (the outcome goes in `memo`); this covers the summary message.",
  "type": "object",
  "required": ["status"],
  "properties": {
    "status": { "type": "string", "enum": ["fixed", "unfixable"] },
    "failure": { "type": "string" }
  }
}
