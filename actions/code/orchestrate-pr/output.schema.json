{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "code.orchestrate-pr output",
  "description": "Reported via submit_step_progress's resolve move; this covers the summary it carries.",
  "type": "object",
  "required": ["summary"],
  "properties": {
    "summary": {
      "type": "string",
      "description": "What shipped, and how the PR settled."
    },
    "prUrl": {
      "type": "string",
      "description": "The PR this step opened and shepherded."
    }
  }
}
