{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "code.orchestrate-pr output",
  "description": "The step's output is the plain string the controller passes as `next.output` on its `resolve` move — `submit_step_progress` types `output` as a string, so there is no object here. By convention it carries the PR URL and one line on what shipped: \"Merged https://github.com/o/r/pull/12: adds the retry queue.\"",
  "type": "string"
}
