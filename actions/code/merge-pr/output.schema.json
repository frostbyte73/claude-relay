{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "code.merge-pr output",
  "description": "Reported via submit_step_progress (the outcome goes in `memo`); this covers the summary message. `remoteBranchDeleted` is best-effort and never gates `merged`.",
  "type": "object",
  "required": ["status"],
  "properties": {
    "status": { "type": "string", "enum": ["merged", "not-merged"] },
    "prUrl": { "type": "string" },
    "remoteBranchDeleted": { "type": "boolean" },
    "failure": { "type": "string" }
  }
}
